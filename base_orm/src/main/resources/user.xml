<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间mapper：com.model.professional -->

<mapper namespace="cn.com.meng.base.orm.IUserDao">
    <select id="load" parameterType="Map" resultType="cn.com.meng.base.domain.User">
        <!-- 底层的SQL语句 -->
        select id,useraccount,password from t_sys_user where useraccount= #{useraccount,jdbcType=VARCHAR} and
        password=#{password,jdbcType=VARCHAR}
    </select>
</mapper>